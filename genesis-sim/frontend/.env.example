# Backend location. Leave unset to use the same origin, which works with the
# Vite dev proxy and with a production build served by the API.
# Set this when the simulator runs on another machine or port:
# VITE_API_URL=http://192.168.1.20:8000
